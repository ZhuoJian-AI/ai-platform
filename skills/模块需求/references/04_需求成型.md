# 04 · 需求成型

> 产出一份**模块需求文档** —— 前面给人看（他和他老板），后面给做的人读。
> 写完这份，这个 skill 的活就干完了，交棒给做的人。

---

## 🔴 最硬的一条：一个技术词都不出现

这份文档里**不许出现**：

> 接口 · API · 数据库 · 字段 · 表 · 服务器 · 部署 · 前端 · 后端 · 框架 · iframe ·
> 组件 · 刷新机制 · 缓存 · 权限模型 · 技术栈 · 架构 · 事件 · 幂等 · 清单协议

**为什么**：
1. 他要拿这份给他老板看、给同事看。出现技术词，这些人就看不懂，也就不会看
2. **技术选型不归我们管** —— 做的那一方会自己定，我们定了反而是添乱
3. 写了技术词，等于替开发做了决定，而我们的判断依据比他少

| ❌ 别写 | ✅ 写成 |
|---|---|
| 数据通过接口实时获取 | 屏上的数字要跟着源头变，源头改了刷新就能看到 |
| 需要提供增删改的 API | 他要能在上面改状态、留话 |
| 数据源为 Excel 文件 | 数据现在记在共享盘那个「生产进度总表」里 |
| 支持按部门做权限隔离 | 生产部的人能改，采购部的人只能看 |
| AI 通过统一操作接口调用 | **员工可以直接跟 AI 说，让 AI 帮着做这个动作** |
| 高风险操作需 requiresConfirmation | **这个动作点下去之前要跳出来问一句"确定吗"** |

⚠️ **唯一例外**：文件名、系统名、路径这些**专有名词**照写（`共享盘 \\10.0.0.33\生产\进度表.xlsx`）——
那不是技术词，是他每天在用的东西的名字。

---

## 文档骨架

**前面一屏读完，后面折叠。**

```
标题：XX模块 · 需求

────────── 前面（一屏读完） ──────────

这个模块解决什么
  一句话：给【谁】在【什么时候】用，用完他会【做什么】

谁参与
  一张表：哪几个部门、各自什么角色、具体做什么

现在是怎么做的
  现状的动作链 + 一个真实事故

要做哪几样
  看板 / 查询 / 操作 / 提醒 / 报告 —— 选中的每样一句话

要能做哪些动作
  谁跟谁互动 · 哪些动作 AI 也能做 · 哪些要先问一句"确定吗"

数据从哪来
  每个数：在哪、谁维护、多久更新一次

什么算做成了
  槽 6 的原话，一字不改

明确不做
  第一版不做的事

还没定的
  待定项 + 为什么定不了 + 谁能定

────────── 后面（折叠，给做的人读） ──────────

每一样的详细说明
数据清单
做完了怎么验（03 那张清单，删掉不适用的整节）
```

---

## 逐节怎么写

### 这个模块解决什么

**格式固定，照填**：

> 这个模块是给【车间的跟单员】在【每天早上上班时】用的，
> 用完他会【决定今天先催哪几家工厂】。

三个方括号全部来自槽 1。**填不满就不要往下写** —— 回去补槽 1。

⚠️ **如果是"给别人用"的情况**，这里写的是**使用者**，不是提需求的人。
另起一行注明：「本需求由【生产部张经理】提出，实际使用者是【跟单员小李等 3 人】。」

### 谁参与 ← 模块制的核心，不能省

**槽 4 那张表直接抄过来**：

| 部门 | 在这个模块里是什么角色 | 具体做什么 |
|---|---|---|
| 生产部 | **牵头** | 录进度、改状态 |
| 设计部 | 协作 | 看进度，出问题时留言 |
| 采购部 | 只看 | 看物料到位情况 |
| 老板 | 审批 | 超期的单子他点确认才能延 |

⚠️ **只有一个部门也要写成一行表格**，不要写成一句话 ——
下游要求参与部门永远是一份名单，即使只有一项。

### 现在是怎么做的

两段：现状的动作链（槽 2 前半）+ 一个真实事故（槽 2 后半）。

> 现在跟单员要知道这些，得打开共享盘那张进度表，筛出本周的，再挨个核对交期，
> 大概二十分钟。有些数表上没有，得去问车间。
>
> 8 月有一单交期晚了三天，是客户打电话来才发现的 —— 表上其实早就写了，
> 但那天没来得及翻。

**这一节是给他老板看的** —— 它解释了为什么要花钱做这个。**别省。**

### 要做哪几样

按 `01` 第三步他选的（A–F），每样一句话：

> **一、打开就能看到今天要催哪几家**（最重要）
> **二、能查某个单号现在到哪一步了**
> **三、能在单子上留一句话给车间**

如果他全选了但排了优先级，**写清楚第一版做哪几样、哪些排到后面** ——
不要写成"以后再说"，写成"**第二期**"，并说明为什么放后面。

### 要能做哪些动作

**三张小表，来自槽 5：**

**谁跟谁互动**
> 跟单员在某一单上留一句话（比如"这单催一下"），车间的人打开时能看到，处理完能标一下"已处理"。

**哪些动作 AI 也能做**

| 动作 | 员工能不能直接跟 AI 说 |
|---|---|
| 查某个单号的进度 | ✅ 可以 |
| 查这周有哪几单要超期 | ✅ 可以 |
| 把某单标成"已处理" | ❌ 要自己上去点 |
| 改交期 | ❌ 要自己上去点 |

⚠️ **这张表必须有**，即使全是"不可以"。没有这张表，做的人会自己猜。
**默认口径**：查可以、改要自己点。

**哪些动作要先问一句"确定吗"**
> 删除记录 · 一次改多条 · 提交给老板审批 —— 这三类点下去之前要跳出来确认，
> 并且要说清楚影响多少条。

### 数据从哪来

| 要用到的这个数 | 现在在哪 | 谁维护 | 多久更新 |
|---|---|---|---|
| 交期 | 共享盘 `生产进度总表.xlsx` | 小王 | 每天下班前 |
| 供应商准时率 | **没有现成的，要从上面那张表算** | — | 跟着上面 |
| 已入库件数 | 仓库那边的系统 | 仓管 | 实时 |

⚠️ **"没有现成的"那几行要特别标出来** —— 算法要跟用户确认过，没确认就是〔待定〕。

### 什么算做成了

**槽 6 的原话，一字不改地抄。** 不要润色、不要改写成"专业"说法。

> 早上打开，三十秒内能看出今天要催哪几家。

### 明确不做

从 `03` 的「明确不做」挑适用的，加上这次特有的。**这一节必须有内容，不能是空的。**

### 还没定的

**每条写三样：是什么 · 为什么定不了 · 谁能定。**

> - **准时率怎么算**：按交期当天到算准时，还是提前也算？张经理说要问一下他们经理。
> - **"这周"从周几算起**：周一还是周日？没问，按周一记的。
> - **车间的人怎么知道有留言**：还是他要自己打开才看得到？没定。

⚠️ **不要把待定项藏起来。** 做的人看到待定会来问，看不到就会自己猜 —— 猜错了返工更贵。

---

## 诚实标注把握度

不懂技术的人会对很多事说「你帮我定」。**这些要标出来，不能混在他真正确认过的内容里。**

| 标记 | 什么意思 | 例子 |
|---|---|---|
| （无标记） | 他明确说的 | 「每天早上上班时用」 |
| **〔我先按这个记〕** | 他说"你帮我定"，我给了默认值**并告诉了他** | 「〔我先按这个记〕AI 只能查不能改」 |
| **〔待定〕** | 两轮都答不出，需要他回头去问 | 「〔待定〕准时率的算法」 |

**收尾时把所有〔我先按这个记〕念一遍给他听**：

> 「有几处我替你定了，你听一下有没有不对的 ——
> 我按【AI 只能查不能改】记的、按【周一算一周的开始】记的……
> 这些以后都能改，现在有觉得不对的吗？」

---

## 完成之后：交棒

需求文档写完，**这个 skill 的活就干完了**。

### 交棒时对用户说什么

> 「需求整理完了，在【文件位置】。
> 接下来我把这份交给做的人，他会照着做出来。做完之后我们拿最后那张清单一条条验。
>
> 你现在要做的只有一件事：**把这份看一遍，
> 特别是我标了〔我先按这个记〕的那几处**，看有没有替你定错的。」

### 移交清单

- [ ] 模块需求文档（这一步的产出）
- [ ] 他给的所有原始材料（Excel、截图、旧系统）
- [ ] 〔待定〕项清单 —— **单独列出来**，别让它埋在文档里

---

## 这一段完成的标志

- [ ] 需求文档写完了，前面一屏能读完
- [ ] **全文搜一遍，没有任何技术词**
- [ ] 「谁参与」那张表有内容，**哪怕只有一个部门也是一行表格**
- [ ] 「哪些动作 AI 也能做」那张表有内容，**哪怕全是"不可以"**
- [ ] 「什么算做成了」是他的原话，一字没改
- [ ] 「明确不做」那节不是空的
- [ ] 所有〔我先按这个记〕都念给他听过了
- [ ] 〔待定〕项单独列了出来
- [ ] 数据来源表里，每行都有"在哪 / 谁维护 / 多久更新"

**最后自检一句**：把这份交给一个完全不了解这摊业务的人，**他能不能不回头问业务问题就开工？**
不能，就是还没写完。
