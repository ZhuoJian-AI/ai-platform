"""DLP 内置模式库 — 医疗信息。

注：原内置医疗规则（ICD疾病编码、医疗记录号、诊断关键词）已下线：
- ICD 编码正则 `\b[A-Z]\\d{2}(?:\\.\\d{1,4})?\b` 误匹配量大（如版本号、坐标），误杀严重；
- 医疗记录号/诊断关键词的命中场景过于宽泛，企业默认围栏不强制启用。
如需恢复，请重新在此定义并在 ALL_MEDICAL_RULES 中注册。
"""

ALL_MEDICAL_RULES: list[dict] = []
