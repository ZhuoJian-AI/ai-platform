# 录音回填生命周期修复（未部署）

- 基线：`9af3691`，复用已认领的 PLATFORM-VOICE-RELIABILITY-20260911。
- 修改：转写结果追加到最新输入值，保留等待期间的输入和空白；录音使用递增代次，取消、工作空间/任务切换和卸载后的旧结果不再回填。
- 麦克风权限请求返回前退出时，返回的轨道立即停止；MediaRecorder 初始化失败也释放轨道。阻止权限请求期间重复开麦。
- 转写期间提供“取消回填”；这是停止前端等待与回填，不声称已经取消服务器任务。
- 验证：`git diff --check` 与 `frontend` 下 `npm run typecheck` 通过。未执行真实麦克风验收，未合并或部署。
- 尚未完成：临时录音存储、后端取消、角色能力提示、消息朗读、共享沉浸式控制器。当前录音仍走已有工作空间上传路径，不能把本修复视为第二批完整交付。
- 不修改模型部署、角色、业务子系统、OSS 配置或服务器数据盘。
