# staging 双端独立浏览器基线验收

- 任务：PLATFORM-UNIFIED-ASSISTANT-RUNTIME-E2E-20260910，Codex。
- 本地源码：9ce1e90，独立修复分支；本轮没有部署该源码。
- 目标站点：https://ai-platform.staging.zhuojianai.com。
- 部署规则：重新获取 c948cd2；本轮不修改 Coolify 配置或镜像。

## 执行方式

在 frontend 运行 `node scripts/e2e-staging-core.mjs`，使用独立 headless Chrome、隔离管理员与员工 Context，通过真实表单登录。凭据仅通过运行时环境传入，无截图、Trace 或凭据落盘。明确清空 E2E_MODEL_ALIAS，未启用脚本中修改正式 API Key 范围的可选分支；国内站点直连。

## 实际结果

进程正常结束，exit 0：

- 管理员错误密码中文提示、正常登录成功。
- 17 个可见管理员导航、4 个直达页面渲染通过；退役入口检查通过。
- 员工错误密码提示、正常登录、工作空间及智能体导航通过。
- 管理员有效权限查询与员工实时有效权限一致，比较覆盖 18 个工作空间及角色。
- 企业应用 iframe 正文、SSO 后页面加载通过。脚本将此步骤命名为 Bridge，但未独立证明每一种 Bridge 事件。
- 当前业务数据生成 Excel，出现文件卡片；实际点击下载取得 XLSX，校验 ZIP 和 OOXML 必需条目。
- 消息 Artifact 存在 fileId/versionId/SHA-256，与工作空间当前版本一致；表格预览 ready 且有工作表。
- 本轮测试对话删除 1 条，测试文件移入回收站 1 个；并非物理删除 OSS。
- 管理员、员工通过界面退出，退出接口返回 204 并跳转登录页。
- 诊断断言未发现本脚本收集范围内的未解释控制台错误、5xx 或登录后的 401/403。

## 证据边界及后续

这只是当前 staging 的核心回归，不是本地修复已上线的证据，也不是所有管理员模块 CRUD、跨入口同一 Task、完整 Bridge、真实语音或生图验收。本脚本没有验证 Excel 行内容与业务数据逐字段一致，仍需补充业务正确性检查。

此前 Playwright 扩展阻塞未通过重启其他窗口解决；独立浏览器可以正常运行，后续可沿用此通道继续真实多模态和统一会话测试。仍需完成音频工具可用性、真实产物、权限撤销、模型协议、发布门禁和部署后回归，不得以此标记整体目标完成。
