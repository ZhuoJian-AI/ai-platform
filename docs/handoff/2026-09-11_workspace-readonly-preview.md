# 网页预览退出手工编辑

- 范围：共用 BrowserDrawer 及管理员工作空间、员工工作空间、助手预览四个调用入口。部署规则 c948cd2。
- 删除文本/Markdown/JSON/CSV 草稿、编辑按钮、保存回调和编辑状态，不删除用户文件、版本或后端文件能力。
- 保留只读渲染、全屏、原件下载、历史版本、版本恢复和文件更新通知。版本恢复不是文档手工编辑，仍受权限和版本约束。
- 新只读契约测试通过；现有 Playwright 文件 UI 回归改为验证拥有完整权限时仍无编辑入口，Markdown/CSV 内容和全屏保留、实际下载成功；HTML 不执行脚本及 Excel 选择复制回归通过。
- 前端类型检查和生产构建通过。这里是本地浏览器组件验证，不冒充已部署或真实管理员/员工登录验证。
- AI 编辑另存尚未修改：builtin_tools.py 目标文件分支仍会替换版本；下一批需处理来源版本检查、独立输出空间、幂等和原件不变。不能将本批网页编辑删除视为完成 AI 编辑策略。

## 后续保存策略实现

- 文件执行器的目标文件现在作为只读源，输出使用任务工作空间的 create 权限和独立文件 ID；执行前后校验源版本，最终重新鉴权。输出记录 derived_from_file_id/version_id。
- 文本更新入口转到受控文本生成并另存；非文本请求返回对应格式工具提示，不允许旧入口覆盖 Office 原件。原始业务 API 的文件版本能力保留。
- 沿用持久化创建幂等；相同输出请求复用结果，目标同名文件冲突不覆盖。生成结果仍经文件服务和 Artifact 验证。
- 6 项新隔离测试及现有纯文件契约合计 52 项通过，Ruff 通过；独立临时 PostgreSQL 上 Excel 新文件/原件版本不变测试 1 项通过（156 秒含建库清库）。测试容器和 SSH 转发已清理，未接触 staging 数据库。
- 本段只代表代码及定向测试通过；实际部署和线上文件验收另行记录。

## staging 发布

- source `38a2688c4154448bc146397def71e80bf5354624`（PR #97），manifest `4e460796d6e918bfbf9cbc32ab67ae2115f794fc`（PR #98）。
- backend `sha256:f93e2db43e3e5e98c8a98853c5df15d4a89420689f1736858f839c71d3d9a463`；frontend `sha256:9bdaf1923243f536b58eef55febc2f515c1754493c46677780f37ba42a7e9a78`。运行容器 digest 和 OCI revision 已核对。
- Coolify `saveas4dd807c7f4d0e254`，2026-09-11 07:05:06 UTC finished。9 服务 healthy，公网 `/health` 200；Compose 和真实环境变量预检通过。
- 无数据库迁移，不修改 OSS/CORS、业务服务器或外部 Skill。旧镜像保留用于回切：backend `38b0543409bfdeb55362f4f74e81f2a3e9e08b01f2471109a301a7d025d5e2f3`，frontend `792fdd1c480bccf5aaaed752b1163227f7e05d7f2432f58424baba0f5e9d7367`。
- 本批真实双端文件验收结果继续追加；部署成功不等于验收完成。

## 真实用户验收与管理员直达链接修复

- 真实员工上传 Markdown，Task `4a2058e3-e804-4c55-975f-90a800d7de79` 经能力搜索、workspace_search、text_edit 成功生成一个 Artifact。原件 `8c77127b-b1a4-439b-b682-1264e4411625` 保留原版本和数量 100；新文件 `03fd32f8-21d3-4890-bf0a-7d3bba84122c` 下载包含 120；两个文件均为 41 字节。
- 员工线上预览、实际下载、无手工编辑入口通过。首次脚本未等待工作空间/附件就绪的问题已修正；运行期间一次 401 后重新真实登录，只读取已有 Task，不重复执行模型修改。该 401 的原因未确认，不能称为全链路无异常。
- 管理员端发现初始化竞态：OrgSelect 自动选择默认组织会清空 URL 的 workspace/file，导致深链抽屉未打开。修复为先完成深链鉴权定位，再挂载组织选择器；失败后仍释放选择器，不改变权限。
- 候选前端配合线上后端，root 真实登录、直达新文件、预览与实际下载通过；此项使用候选静态资源，不冒充已部署验证。类型检查、构建、只读契约和真实浏览器定向验收通过。修复部署结果另补。
- 测试文件已移入回收站，可恢复；没有删除真实文件。复测恢复的只是上述两个明确测试文件，未重复调用 LLM。
