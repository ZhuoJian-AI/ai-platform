# 本地前端与真实接口组合验收

任务：PLATFORM-UNIFIED-ASSISTANT-RUNTIME-E2E-20260910。

- 上一目标轮为 progress：有前端修复提交和真实续问证据。本轮保持完整目标，不把单项通过作为整体完成。
- 新增 `E2E_LOCAL_FRONTEND=1`，仅在测试浏览器内用当前 `dist` 替换 SaaS 静态文件。所有 API、真实登录、模型、子系统和 OSS 请求不替换；不部署、不更改服务器。默认关闭；输出明确标注不是已部署前端验收。
- 静态文件限制在真实 dist 目录，拒绝目录穿越及解析后越界文件。脚本语法检查通过。
- handle 45650 已 exit 1：真实管理员、员工登录、导航、18 工作空间权限一致性和 iframe 渲染通过；新建 Task 后导航事件的 `hasConversation=true`，说明本地 URL 修复已实际执行。
- 明确要求 Excel（.xlsx）仍得到 CSV（1758 字节），正确判定失败。控制台、5xx、鉴权错误记录均为空。清理报告 Task 1、文件移入回收站 1、failures=[]。
- 尚未进入刷新和跨视图阶段，不能宣称刷新通过。下一步应验证包含本地后端格式修复的环境，不应反复运行旧后端来碰运气获得 XLSX。
- 没有存活测试句柄；未发布、未修改线上配置、外部 Skill 或业务系统。完整模型能力交付与发布门禁仍未完成。
